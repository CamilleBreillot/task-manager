<%= simple_form_for(task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% unless f.object.new_record? %>
    <%= f.label "Completed" %>
    <%= f.check_box :completed %>
  <% end %>
  <%= f.submit %>
<% end %>
